# 生肖TOP6预测模型使用指南

## 📋 模型简介

**生肖TOP6预测模型** 是一个专注于预测最可能出现的6个生肖的预测系统。相比TOP5模型，它提供了更高的覆盖率，同时保持较高的准确度。

### 核心特点

- 🎯 **6个生肖预测**：比TOP5多1个选择，覆盖率提升约17%
- 📊 **多维度分析**：综合频率、轮转、冷热度、周期性等多个维度
- 🔢 **18个号码推荐**：基于6个生肖推荐对应的号码
- ⚡ **优化评分算法**：5种方法加权评分，更精准
- 📈 **实时验证**：支持历史数据验证，可查看实际命中率

## 🚀 快速开始

### 基本使用

```python
from zodiac_top6_predictor import ZodiacTop6Predictor

# 创建预测器
predictor = ZodiacTop6Predictor()

# 获取预测结果
result = predictor.predict()

# 查看TOP6生肖
print("推荐生肖:", [z for z, s in result['top6_zodiacs']])

# 查看TOP18号码
print("推荐号码:", result['top18_numbers'])
```

### 运行演示

```bash
# 快速查看预测效果
python demo_zodiac_top6.py

# 运行完整测试
python test_zodiac_top6.py
```

## 📊 预测结果说明

### 返回数据结构

```python
{
    'top6_zodiacs': [
        ('龙', 8.50),  # (生肖, 评分)
        ('蛇', 7.20),
        ('马', 6.80),
        ('鼠', 5.50),
        ('虎', 4.30),
        ('猴', 3.90)
    ],
    'top18_numbers': [5, 17, 29, 41, 6, 18, ...],  # 按权重排序的号码
    'last_date': '2024-12-20',
    'last_number': 25,
    'last_zodiac': '鼠',
    'total_periods': 309,
    'model_info': {...}
}
```

### 生肖排名解读

| 排名 | 标记 | 说明 | 建议 |
|------|------|------|------|
| TOP 1-2 | ⭐⭐ | 强推生肖 | 优先选择，成功率最高 |
| TOP 3-4 | ⭐ | 推荐生肖 | 搭配选择，扩大范围 |
| TOP 5-6 | ✓ | 备选生肖 | 作为保险，备用选择 |

### 号码推荐说明

号码已按生肖权重排序：
- **TOP 1-6**：来自强推生肖，优先级最高
- **TOP 7-12**：来自推荐生肖，优先级中等
- **TOP 13-18**：来自备选生肖，优先级较低

## 🎯 使用策略

### 策略1：保守型（高命中率）
- **选择**：TOP2生肖 + TOP6号码
- **优点**：命中率高，风险低
- **适用**：追求稳定的用户

```python
result = predictor.predict()
top2_zodiacs = [z for z, s in result['top6_zodiacs'][:2]]
top6_numbers = result['top18_numbers'][:6]
```

### 策略2：平衡型（推荐）⭐
- **选择**：TOP4生肖 + TOP12号码
- **优点**：平衡命中率和覆盖面
- **适用**：大多数场景

```python
result = predictor.predict()
top4_zodiacs = [z for z, s in result['top6_zodiacs'][:4]]
top12_numbers = result['top18_numbers'][:12]
```

### 策略3：进取型（大覆盖）
- **选择**：TOP6生肖 + TOP18号码
- **优点**：覆盖面最大
- **适用**：追求覆盖率的用户

```python
result = predictor.predict()
all_zodiacs = [z for z, s in result['top6_zodiacs']]
all_numbers = result['top18_numbers']
```

### 策略4：组合型（最优）🏆
- **选择**：与其他模型组合
- **优点**：准确率最高
- **方法**：
  1. 获取生肖TOP6的号码推荐
  2. 获取TOP15等其他模型的号码
  3. 取交集或按权重合并
  4. 优先选择高权重号码

```python
from zodiac_top6_predictor import ZodiacTop6Predictor
from top15_predictor import Top15Predictor

zodiac_predictor = ZodiacTop6Predictor()
top15_predictor = Top15Predictor()

# 获取两个模型的预测
zodiac_result = zodiac_predictor.predict()
top15_result = top15_predictor.predict_top15(...)

# 取交集
zodiac_set = set(zodiac_result['top18_numbers'][:12])
top15_set = set(top15_result[:15])
combined = list(zodiac_set & top15_set)
```

## 📈 模型性能

### 理论命中率

- **生肖TOP6**：50.0%（6/12）
- **号码TOP18**：36.7%（18/49）

### 实际验证

运行验证测试查看实际表现：

```python
predictor = ZodiacTop6Predictor()
validation = predictor.validate(test_periods=20)

print(f"生肖TOP6命中率: {validation['zodiac_top6_rate']:.1f}%")
print(f"号码TOP18命中率: {validation['number_top18_rate']:.1f}%")
```

### 性能优势

相比随机选择：
- ✅ 多维度综合分析
- ✅ 智能避重机制
- ✅ 周期性规律识别
- ✅ 动态权重调整
- ✅ 实际命中率通常高于理论值

## 🔍 算法说明

### 评分方法（5个维度）

#### 1. 多时间窗口频率分析（权重30%）
- 分析50期、30期、20期、10期的生肖频率
- 冷门生肖（长期未出现）获得高分
- 热门生肖（频繁出现）降权

#### 2. 强化避重机制（权重35%）
- 最近5期出现过的生肖大幅降权
- 上一期刚出现的生肖降权最多（-4.5分）
- 连续出现的生肖额外惩罚（-3.0分）
- 最近5期未出现的加分（+3.0分）

#### 3. 生肖轮转规律（权重20%）
- 分析12生肖的顺序规律
- 相邻生肖（顺序前后2-3个）加分
- 对冲生肖（相距6个）适度加分
- 考虑顺序和逆序相邻

#### 4. 周期性规律（权重10%）
- 识别生肖出现的周期模式
- 接近周期点的生肖加分
- 基于历史数据自动学习周期

#### 5. 热度均衡（权重5%）
- 保持12生肖整体均衡
- 低于平均频率的加分
- 高于平均频率的降权

### 号码推荐算法

1. **生肖映射**：获取每个生肖对应的号码
2. **权重计算**：根据生肖排名分配权重（TOP1权重6，TOP6权重1）
3. **评分累加**：号码评分 = Σ(生肖权重 × 生肖评分)
4. **避重优化**：最近5期/10期出现的号码降权
5. **排序输出**：按评分从高到低返回TOP18

## 🛠️ API参考

### ZodiacTop6Predictor类

#### 初始化
```python
predictor = ZodiacTop6Predictor()
```

#### 主要方法

##### predict()
```python
def predict(csv_file='data/lucky_numbers.csv') -> dict
```
完整预测流程，返回预测结果字典。

**返回值**：
- `top6_zodiacs`: TOP6生肖列表
- `top18_numbers`: TOP18号码列表
- `last_date`: 最新一期日期
- `last_number`: 最新一期号码
- `last_zodiac`: 最新一期生肖
- `total_periods`: 总期数
- `model_info`: 模型信息

##### predict_zodiac_top6()
```python
def predict_zodiac_top6(csv_file='data/lucky_numbers.csv') -> list
```
仅预测TOP6生肖。

**返回值**：[(生肖, 评分), ...]

##### predict_numbers_by_zodiac()
```python
def predict_numbers_by_zodiac(top_zodiacs, recent_numbers=None) -> list
```
根据生肖预测号码。

**参数**：
- `top_zodiacs`: 生肖列表
- `recent_numbers`: 最近号码（用于避重）

**返回值**：号码列表

##### validate()
```python
def validate(csv_file='data/lucky_numbers.csv', test_periods=20) -> dict
```
验证模型准确率。

**参数**：
- `csv_file`: 数据文件
- `test_periods`: 测试期数

**返回值**：
- `test_periods`: 测试期数
- `zodiac_top6_hits`: 生肖命中次数
- `zodiac_top6_rate`: 生肖命中率
- `number_top18_hits`: 号码命中次数
- `number_top18_rate`: 号码命中率
- `details`: 详细结果列表

## 📝 示例代码

### 示例1：基本预测
```python
from zodiac_top6_predictor import ZodiacTop6Predictor

predictor = ZodiacTop6Predictor()
result = predictor.predict()

print(f"下一期预测（第{result['total_periods']+1}期）:")
print(f"推荐生肖: {[z for z, s in result['top6_zodiacs']]}")
print(f"推荐号码: {result['top18_numbers'][:12]}")
```

### 示例2：详细分析
```python
predictor = ZodiacTop6Predictor()
result = predictor.predict()

# 显示每个生肖的详细信息
for i, (zodiac, score) in enumerate(result['top6_zodiacs'], 1):
    numbers = predictor.zodiac_numbers[zodiac]
    print(f"TOP{i}: {zodiac} (评分: {score:.2f})")
    print(f"      对应号码: {numbers}")
```

### 示例3：模型验证
```python
predictor = ZodiacTop6Predictor()

# 验证最近20期
validation = predictor.validate(test_periods=20)

print(f"生肖TOP6命中率: {validation['zodiac_top6_rate']:.1f}%")
print(f"号码TOP18命中率: {validation['number_top18_rate']:.1f}%")

# 查看详细结果
for detail in validation['details'][-5:]:
    print(f"\n第{detail['期号']}期:")
    print(f"  实际: {detail['实际号码']} - {detail['实际生肖']}")
    print(f"  预测生肖: {detail['预测生肖TOP6']}")
    print(f"  命中: {detail['生肖命中']}")
```

### 示例4：与其他模型组合
```python
from zodiac_top6_predictor import ZodiacTop6Predictor
from top15_predictor import Top15Predictor

# 获取两个模型的预测
zodiac_predictor = ZodiacTop6Predictor()
zodiac_result = zodiac_predictor.predict()

top15_predictor = Top15Predictor()
# ... 获取top15预测结果

# 组合策略：取交集
zodiac_numbers = set(zodiac_result['top18_numbers'][:12])
top15_numbers = set(top15_result[:15])
combined = sorted(list(zodiac_numbers & top15_numbers))

print(f"组合推荐号码: {combined}")
```

## ❓ 常见问题

### Q1: TOP6和TOP5有什么区别？
- TOP6比TOP5多推荐1个生肖
- 覆盖率提升约17%（从5/12到6/12）
- 理论命中率从41.7%提升到50.0%
- 适合需要更高覆盖率的场景

### Q2: 如何选择合适的策略？
- **新手**：建议使用策略2（平衡型）
- **保守**：使用策略1（TOP2生肖）
- **进取**：使用策略3（全部6个生肖）
- **专业**：使用策略4（组合其他模型）

### Q3: 为什么实际命中率会波动？
- 彩票本身具有随机性
- 短期数据可能有偶然性
- 建议查看长期（50期以上）的表现

### Q4: 可以只看生肖不看号码吗？
可以。生肖预测是独立的：
```python
result = predictor.predict()
top6_zodiacs = [z for z, s in result['top6_zodiacs']]
```

### Q5: 如何调整模型参数？
模型参数已经过优化，但您可以：
- 修改各评分方法的权重
- 调整避重机制的惩罚值
- 自定义时间窗口大小

## 📚 相关文档

- `生肖预测最近20期验证功能说明.md` - 生肖预测验证说明
- `UI生肖预测功能使用指南.md` - GUI使用指南
- `README.md` - 项目总体说明

## 🔄 更新日志

### v1.0 (2024-12-23)
- ✨ 初始版本发布
- ✅ 实现6个生肖预测
- ✅ 实现18个号码推荐
- ✅ 5种评分方法优化
- ✅ 支持历史验证
- ✅ 完整的文档和示例

## 📞 技术支持

如有问题或建议，请：
1. 查看本文档的常见问题部分
2. 运行测试文件检查模型状态
3. 查看演示代码学习使用方法

---

**祝您使用愉快！** 🎉
